package pl.ttpsc.solid.ocp.usa.solution;

public class USASpeedLimitFines {

    public double calcualateSpeedLimitFine(SpeedLimit speedLimit, int speed) {

        double fine = 0.0;
        if (speed > speedLimit.getMaxSpeed()) {
            // calculate...
        }
        return fine;
    }

}
